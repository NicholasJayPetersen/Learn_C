//Nicholas Petersen

#include <stdio.h>

int main()
{
	//Logical operators not and or ! && ||
	int weight = 15;
	float price;
	if (weight > 2 && wieght < 6)
		price = 3.75;
	else 
		price = 8;
		
	
	//short circuit evaluation - evaluates first expression and quits if false.
	//order matters for efficiency
	bool invite;
	if (city =="Dearborn" && major =="CS")
		invite = true;
	else
		invite = false;
		
	//character comparison based on ASCII values
	if ('A' > 'C')
	// 65 > 67 is false
	
	return 0;
}
